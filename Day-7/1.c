// impliment stack using array

#include <stdio.h>
int main(){
    int size;
    printf("enter size");
    scanf("%d",&size);
    int arr[size];
    for(int i=0;i<size;i++){
        printf("Print array",arr[i]);  
    }
    
}